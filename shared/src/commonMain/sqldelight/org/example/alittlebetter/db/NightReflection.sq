CREATE TABLE nightReflection (
    id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    date TEXT NOT NULL,
    question TEXT NOT NULL,
    answer TEXT NOT NULL
);

insertAnswer:
INSERT INTO nightReflection(date, question, answer) VALUES (?, ?, ?);

selectForDate:
SELECT * FROM nightReflection WHERE date = ?;
